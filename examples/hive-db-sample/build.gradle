apply plugin: "gradle-hivemigration-plugin"

buildscript {
	repositories {
		mavenCentral()
		mavenLocal()
	}
	dependencies { 
		classpath "ar.fb.gradle.plugins.hivemigration:gradle-hivemigration-plugin:0.0.1" 
	}
}

hivemigration {
	url="jdbc:hive2://localhost:10000"
	schema="STAGE_\${ENV}"
	ENV=findProperty("ENV")     		// Reads argument -PENV=xxx
	user=findProperty("user")			// Reads argument -Pusername=xxx
	password=findProperty("password")	// Reads argument -Ppassword=xxx
}

